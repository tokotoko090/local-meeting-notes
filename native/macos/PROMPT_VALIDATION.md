# プロンプト編集の検証

2026年9月18日実施。

## 自動検証

- `.venv-mlx/bin/python -m unittest discover -s tests -v`: 34件成功。標準出力との完全一致、設定保存、処理開始時のテンプレート固定、過去ファイルの維持、空欄・処理中・存在しないファイルの拒否、原子的保存の失敗、コピーのみ失敗した場合の通知を含む。
- `.venv-mlx/bin/python scripts/smoke-http.py`: 実HTTP API経由でテンプレート設定、読み込み、保存、空欄拒否、設定と会議ファイルの分離を確認。
- `node_modules/.bin/electron scripts/verify-prompt-ui.cjs`: 13件成功。共通設定の編集・保存・標準復元・破棄、個別編集の保存・コピー、読み込み失敗、保存失敗時の草稿保持、コピーのみ失敗、録音中ロック、未保存・保存中フラグ、繰り返しEscapeを確認。
- ライト／ダーク、1040×840／860×640、長い日本語Markdownで編集画面を描画。結果は `build/prompt-ui-validation/report.json`、画像は同フォルダ。

設定と個別編集のダイアログはEscapeをキーイベント段階で処理し、繰り返し押してもブラウザの標準ダイアログ終了によって未保存内容を失わないようにした。編集へ戻る操作ではテキスト欄へフォーカスを戻す。

## 完成Macアプリでの操作

同梱Pythonバックエンドと画面を再ビルドし、アプリの署名検証に成功。ユーザーの設定とは別の検証用設定フォルダで起動した。

1. 設定画面で日本語の共通テンプレートへ変更。
2. 未保存のままCommand+Qを押し、ネイティブの破棄確認を表示。「編集を続ける」で草稿を保持。
3. 共通テンプレートを保存し、保存済み音声の検証フォルダを指定。
4. キャッシュ済みlarge-v3-turboを使い、ネット接続なしで日本語のマイク・再生音をMLXで文字起こし。
5. 完了画面からプロンプトを開き、カスタムテンプレートに文字起こし全文が自動追加されたことを確認。
6. 会議ごとの補足を加え、「保存してコピー」。保存ファイルとクリップボードの515文字が完全一致し、共通テンプレートには補足が混入していないことを確認。
7. 最終版へ再起動し、テンプレートが保持されることを確認。未保存編集後にEscape→編集に戻る→Escape連打でも草稿が残り、明示的な破棄操作だけで閉じることを確認。

個別編集の保存成功画面、ライト／ダークの最小サイズ編集画面を目視し、文字・ボタンの重なりや横方向のはみ出しがないことを確認した。

Windowsのネイティブクリップボードやインストーラーは実機未検証。共通バックエンドとWindows設定機能の回帰テストで互換性を確認する。

## 最終UI回帰

プロンプト専用の `scripts/verify-prompt-ui.cjs` は13/13成功、既存の `scripts/verify-ui.cjs` は93/93成功。録音からコピー・終了までの既存フロー、Windows設定、4サイズ・ライト／ダークの表示、設定のフォーカス巡回を含めて回帰なし。新エディターでは未保存確認の繰り返し、Tab巡回、閉じた後のフォーカス復帰、保存失敗／コピー失敗後の草稿保持、保存中ロックも確認した。

結果: `build/prompt-ui-validation/report.json`、`build/ui-validation/report.json`。長いMarkdownの1040×840・860×640両テーマ画像は `build/prompt-ui-validation` に保存し、描画待機後に撮影して目視確認済み。
